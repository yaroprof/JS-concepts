// - Use the rest operator to gather all remaining arguments in a function and return the sum of those arguments.

// const sum = ( first, second, ...sum) => {
//   console.log(sum.reduce((total, num) => total + num));
// }
// sum(1,2,3,4,5)

